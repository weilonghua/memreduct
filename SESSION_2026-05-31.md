# Mem Reduct session archive

Date: 2026-05-31

## Scope

- Confirmed `C:\Program Files\Mem Reduct` is the installed release directory, not the source tree.
- Continued work in `C:\Users\魏隆华\.codex\memories\upstream`.
- Used sibling dependency checkouts:
  - `C:\Users\魏隆华\.codex\memories\routine`
  - `C:\Users\魏隆华\.codex\memories\builder`

## Completed

- Preserved the earlier tray icon fixes:
  - restore `SetBkMode` on the correct DC;
  - keep the previous tray icon when `CreateIconIndirect` fails.
- Added a single cleanup-in-progress gate across cleanup entry points.
- Split memory cleanup into preparation, core cleanup execution, completion reporting, and async start helpers.
- Kept command-line cleanup synchronous.
- Moved UI-triggered cleanup paths to the async worker:
  - timer auto cleanup;
  - hotkey cleanup;
  - tray mouse action cleanup;
  - tray menu region cleanup;
  - main clean button and tray clean command.
- Added `RM_CLEANUPDONE` (`WM_APP + 12`) to marshal cleanup completion back to the UI thread.
- Added local compatibility shims for the current sibling `routine` config API.
- Replaced the missing mount-manager helper path with logical-drive volume flushing so this checkout builds with the available `routine` and SDK.
- Fixed build warnings treated as errors in menu generation and popup positioning.

## Verification

- Release x64 build passed:
  - `vcvarsall.bat amd64`
  - `MSBuild.exe memreduct.sln -property:Configuration=Release -property:Platform=x64 -verbosity:minimal`
  - Output: `bin\64\memreduct.exe`
- Release ARM64 build passed:
  - `vcvarsall.bat amd64_arm64`
  - `MSBuild.exe memreduct.sln -property:Configuration=Release -property:Platform=ARM64 -verbosity:minimal`
  - Output: `bin\ARM64\memreduct.exe`
  - Existing non-fatal warning remains from `routine`: `Undefined: IDS_UPDATE_AUTOINSTALL`.
- `git diff --check -- src/main.c src/main.h` passed.

## Current modified files

- `src/main.c`
- `src/main.h`
- `SESSION_2026-05-31.md` (untracked archive file)

## Follow-up candidates

- Consider moving compatibility shims into a cleaner versioned dependency update if `routine` publishes the newer API expected by `main.c`.
- Consider adding a stronger UI busy state beyond the wait cursor, such as disabling clean commands while `cleanup_in_progress` is set.
- Timer/list redraw throttling and config caching were not tackled beyond the cleanup-path changes.
